<% rounded(:class => "tips") do %>	

 
 <% form_tag(search_by_tag_path(@space.id, tag), :method => :get) do %>
      <%= text_field_tag :tag, params[:tag] %>
	  <%= submit_to_remote 'Search', search_by_tag_path(@space.id, tag), :method => :get%>
<%= link_to_remote 'Hide' , {:url=>{:action=> 'clean'}}%>

 
 <% end %>
  <% end %>