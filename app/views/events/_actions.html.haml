.content-block
  .content-block-header
    %h3= t("action.other")
  .content-block-middle
    - if @event.future? || @event.is_happening_now?
      = link_to t('invitation.title'), inviteroom_invite_path(:type => "event", :event => @event.id), :class => 'open-modal btn btn-small'
      = link_to t('event.notification.button'),sendnotification_invite_path(:event_id => @event.id), :class => 'btn btn-small'
