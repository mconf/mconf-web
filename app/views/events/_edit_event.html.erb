<% content_for :javascript do %>
  $(document).ready(function() {
  	$("#change-event").effect("highlight", {color:"#F5DF51"}, 3000);
  })
<% end %>
<% content_for :headers do  -%>
  <%= javascript_include_tag 'user_filter'%>
<% end -%>

<div id="change-event">
<h3>Edit Event</h3>

<% form_for([@space,event], :html => { :multipart => true}) do |f| -%>
  <%= f.error_messages %>				
  <div>
    <label for="Name">Name:</label>
    <%= f.text_field :name, :size =>25  %>
  </div>
  <div>
	<label for="Description">Description:</label>
	<%=f.text_area :description  %>
  </div>
	<div>
		Timezone: <%=Time.zone%>
	</div>
  <div class="no_javascript">
    <ul>
      <li>Start Date</li>
	  <li> <%=f.datetime_select :start_date%></li>
   	  <li>Start Hour </li>
	  <li><%=f.datetime_select :end_date%></li>
	</ul>
  </div>
  <div class="checkbox_event">
		<%=f.check_box :isabel_event,:class=>"icon"  %>
		<label for="isabel_event" class="label_izquierda" >Isabel event:</label>
	</div>
	<div class="checkbox_event">
		<%=f.check_box :marte_event ,:class=>"icon" %>
		<label for="marte_event" class="label_izquierda" >Online event:</label>
	</div>
	 <div>
	 
	 <div id="already_invited_users">
	 <%unless @invited_candidates.empty? && @invited_emails.empty?%>  
	 Users already invited:<br/>
	   <% for event_inv in @invited_candidates %>
	 	<%=event_inv.candidate.name%>
	   <%end%>
	   <% for event_inv in @invited_emails %>
	 	<%=event_inv.email%>
	   <%end%>
	 <%end%>
	 </div>
   <div id="selected_users">
    <label for="selected_users" style="display: none;">To:</label>
  </div>
  <div id="user_filter" style="display: none;">
    <label for="User filter">User filter</label><br/>
    <%= text_field_tag :user_selector,'', :size => 25 %>
  </div>
  <div id="unselected_users"> 
  <% for user in @users_in_space_not_invited -%>
      <div class=user_checkbox>
        <%= check_box_tag "event[ids][]", user.id, false %><%= label("checkbox",sanitize(user.full_name))%><br />
      </div>
  <%end%>
  </div>
	
	
	<%if current_user.superuser? or @space.role_for?(current_user, :name => 'Admin')%>
	  <div>
	    <label for="e-mails" class="label_izquierda" > Invite people from Email (separated by commas):</label>
			<%= f.text_area :mails%>
		</div>	
	<%else%>
	 <div>
	       	
	 </div>	
	<%end%>	
	<div>
    <label for="Notes">Remark:</label>
    <%=f.text_area :notes%>
  </div>
	<div class="margin-up">
	<%= image_submit_tag "buttons/update.png" %>
	</div>  	

<% end %>
</div>