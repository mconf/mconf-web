

 Search by Event's Title
 <% form_tag({:action => :search_by_title}, :method => 'get') do %>
      <%= text_field_tag :query, params[:query] %>
	  <%= submit_tag 'Search'%><br>
  <%= link_to_remote 'Hide',{:url =>{ :action=> 'clean'}}%>
 
 <% end %>
  