<%if @success_update%>
  $("div.message_div#<%=@private_message.id%>").removeClass("highlight");
	$("#user_inbox_link").text("Inbox (<%=PrivateMessage.inbox(current_user).select{|msg| !msg.checked}.size%>)")
<%else%>
  alert("Error updating private message");
<%end%>