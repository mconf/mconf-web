= render :partial => 'menu'

#manage
  #users
    = text_field_tag :users_filter_text
    #users-list
      = render :partial => 'users_list', :locals => { :users => @users }

- nav = page_navigation_links @users
- unless nav.nil?
  .content-block-footer= nav
