<h1>New <%= @content.class.to_s %></h1>

<% unless error_messages_for(:post).blank? && error_messages_for(:content).blank? -%>
<div class="errors">
  <%= error_messages_for :post %>
  <%= error_messages_for :content %>
</div>
<% end -%>

<% form_for(:post, :url => @collection_path, :html => { :multipart => @content.content_options[:has_attachment] }) do |f| -%>

  <%= render :partial => "posts/form", :locals => { :f => f } %>

  <%= submit_tag "Create" %>

<% end -%>
