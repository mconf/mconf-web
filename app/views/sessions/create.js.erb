<%
	redirected_url =  if logged_in? && current_user.chat_activation
                      url_for :controller => "p", :action => "index", :only_path => true
                    elsif logged_in? && !current_user.chat_activation
                      home_path
                    else 
                      # Case: !logged_in?
                      url_for :controller => "sessions", :action => "new", :only_path => true
                    end
%>
window.location.replace("<%= redirected_url -%>");