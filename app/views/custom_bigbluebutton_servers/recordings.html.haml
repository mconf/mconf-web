= render :partial => 'manage/menu'

= content_for :sidebar do
  = render 'shared/webconf_index_links'

%h3= t('.list_of_recordings', :server => @server.name)
= render 'custom_bigbluebutton_recordings/recordings', :recordings => @recordings
