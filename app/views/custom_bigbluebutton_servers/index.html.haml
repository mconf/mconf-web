= render :partial => 'manage/menu'
= content_for :sidebar do
  = link_to t('bigbluebutton_rails.view_room_list'), bigbluebutton_rooms_path, :class => 'btn btn-block'
  = link_to t('bigbluebutton_rails.new_server'), new_bigbluebutton_server_path, :class => 'btn btn-block'
  = link_to t('bigbluebutton_rails.new_room'), new_bigbluebutton_room_path, :class => 'btn btn-block'

#manage
  - @servers.each do |server|
    = render 'custom_bigbluebutton_servers/show', :server => server
