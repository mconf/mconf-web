<%if @events ==  nil %>
<%= t('event.none_coming') %>
<%else %>
	<%if @events.size != 0%>
       		<%=generate_event_table%> 
			<% @events.each do |event| %>
				<div class="summary">
    				<%=show_event(event)%> 
				</div>
			<%end %>
	<%end %>
<%end %>