<%= t('e-mail.dear', :name => @name) %>

<%= t('invitation.space', :space => @space.name) %>

<%= t('invitation.to_accept', :url => invitation_url(@invitation, :host => Site.current.domain)) %>

<%= t('e-mail.copy_link', :url => invitation_url(@invitation, :host => Site.current.domain)) %>

<%= t('invitation.info_space', :space_url => "http://" + Site.current.domain.to_s + "/spaces/"+@space.name) %>

<%= t('e-mail.kind_regards') %>

<%= @invitation.introducer.login%>
<%= @invitation.introducer.email %>
<%= @invitation.introducer.organization%>
