<%= t('hello') %> 
     
   <%= t('received_message_more_machines', :user => @mail_info["from_name"]) %>
     
   <%= t('his_message_reads_thus') %>:
     
    <%= @mail_info["message"] %>
     
    <%= t('consider_and_reply') %>
	<%= t('user.vcc_email', :dir => @mail_info["user_email"]) %>
	<%= t('email.used_contact'), :dir => @mail_info["from_email"]) %>
