= render_page_title t('faq.title')

%ol#faq-topics
  - for i in (0..7) do
    %li
      %h4
        %a{:href => "#faq#{i}"}= t("faq.faq#{i}.question")

%ul#faq-answers
  - for i in (0..7) do
    %li
      %a{:name => "faq#{i}"}
      %h4.question= t("faq.faq#{i}.question")
      .answer
        %p= t("faq.faq#{i}.answer").html_safe
