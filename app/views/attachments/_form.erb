  <!--las 3 líneas del if mirar con más detalle si son prescindibles  -->
  <p>
	<%if @content.id != nil%>
	<%@entry = Entry.find(:first , :conditions => {:content_id => @content.id,:content_type => "Attachment"})%>
	<%end%>
	
    <b>Text</b><br />
<%= fckeditor_textarea(:entry, :description, :toolbarSet => 'Simple', :width => '685px', :height => '400px') %> 
 <%= render :partial => 'attachments/attach' %>
 <div>
 <%content_type = get_attachment_content_type(@entry)%>
<%image = get_attachment_image(content_type)%>
	<p>Attachment:<%= image_tag(image)%><%=@entry.content.size/1024%> Kb</p>
 </div>
    </p>


