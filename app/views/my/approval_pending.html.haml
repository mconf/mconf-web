= render_page_title t('.title')

%h3.description
  = t('.description')

.interesting-links
  %h4= t('.interesting_links')

  %ul
    - if Mconf::Modules.mod_enabled?('events')
      %li=link_to t('.events_list'), mweb_events.events_path

    %li=link_to t('.spaces_list'), spaces_path
