#sidebar-leave-space
  - link = space_performance_path(@space, @space.stage_performances.find_by_agent_id(current_user))
  = link_to t('space.leave'), link, :method => :delete, :title => t("tooltip.leave_space"), :confirm => t('space.confirm'), :class => 'tooltipped upwards'
