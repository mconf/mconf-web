$('#add_entry<%=@day%>').parent().append("<%=escape_javascript(render :partial => 'new')%>");
<%= render :partial => 'agenda_entries/timepicker.js.erb' %>
var destination_add<%=@day%> = $('#new_entry_anchor').offset().top;
$("html:not(:animated),body:not(:animated)").animate({ scrollTop: destination_add<%=@day%>}, 1100, function() {
  window.location.hash = '#new_entry_anchor'; 
});

<%= yield :agenda_attachments%>
 $("#agenda_entry_attachments_attributes_0__tags").fcbkcomplete({
      cache: true,
      filter_case: false,
      filter_hide: true,
      firstselected: true,
      filter_selected: true,
      maxshownitems: 4,
      newel: true,
      complete_opts: true
  }); 

$(".newAttach").livequery('click',function () { 
  var new_id = new Date().getTime(); 
  $(this).before( attachment.replace(/NEW_RECORD/g, new_id) );

  // Applies tags to the attachment
  $("#agenda_entry_attachments_attributes_" + new_id + "__tags").fcbkcomplete({
      cache: true,
      filter_case: false,
      filter_hide: true,
      firstselected: true,
      filter_selected: true,
      maxshownitems: 4,
      newel: true,
      complete_opts: true
  }); 
}); 
