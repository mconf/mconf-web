- if user_signed_in?

  #main-chat-area
    #chat-bar

  //- TODO: remove from here, try to use something like
  //-       gon (https://github.com/gazay/gon)
  :javascript
    var chat_enabled = #{current_site.chat_enabled};
    if (chat_enabled){
    var cookie = "AuthByCookie>>" + "#{cookies[Rails.application.config.session_options[:key]]}";
    var login = "#{current_user.username}";
    var name = "#{current_user.full_name}";
    var url = "#{join_webconf_url(current_user.bigbluebutton_room)}";
    var domain = "#{current_site.presence_domain}";
    var xmpp_server = "#{current_site.xmpp_server}";
    }
