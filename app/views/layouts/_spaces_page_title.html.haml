- if space && space.id != nil
  .current-page-logo
    = logo_image(space, :size => "84x64", :title => space.name)
  .current-page-title
    - if space.name.size > 30
      .small-title
        = sanitize(first_words(space.name, 30))
    - else
      = sanitize(space.name)
