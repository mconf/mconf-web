#footer-wrapper.simple-footer
  #footer
    .link-group
      #footer-logo.link-group
        = link_to current_site.name, "/", :class => 'title'
        %span= t('.copyright', site: current_site.name, year: Time.now.year)
    .link-group.languages
      %h3= t('.translate.change')
      %ul
        - available_locales.each do |locale|
          %li
            - flag = image_tag("flags/" + locale.to_s + ".png", :title => locale.to_s)
            - name = locale_i18n(locale)
            - link = link_to(name, main_app.session_locale_path(:l => locale.to_s), :method => :post)
            = (flag + " " + link).html_safe
