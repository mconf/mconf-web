<% form_tag(search_users_path(@space), :method => 'get') do %>
		<%= text_field_tag 'query',(params[:query] ? params[:query] :  t('user.search'))%>
	  <button type="submit"><%=t("button.go")%></button>
  <% end %>