<div class="user">
	<p>
		<%= link_to "Send a message", new_user_message_path(current_user, :receiver => detailed_user)%><br />
		<a>e-mail</a>
	</p>
        <%=link_icon(detailed_user, :size => 72)%>
	<h5><%=sanitize(detailed_user.full_name) %></h5>
	<%=sanitize(detailed_user.organization) %><br />
	<%=sanitize(detailed_user.city) %>, <%=sanitize(detailed_user.country) %>
</div>
