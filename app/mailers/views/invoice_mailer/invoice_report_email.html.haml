.header_image
  = image_tag URI.join(root_url, "assets/mailer/invoices.png"), size: '544x263'
%p.content
  = t('.message.header', name: @user.name).html_safe

%p= t('.message.link', url: user_subscription_url(@user.username)).html_safe