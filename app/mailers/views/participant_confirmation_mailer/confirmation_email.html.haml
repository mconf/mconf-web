#confirmation-email
  .header
    = @subject

  %p= t('.message', :email => @email, :event => @event)

  %p.link
    = t('.click_here', :url => participant_confirmation_url(token: @pc.to_param, host: Site.current.domain)).html_safe