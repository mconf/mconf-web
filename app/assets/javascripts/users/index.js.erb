//= require user_filter

$(document).ready(function() {

  $("a#new_private_message_link").fancybox({
    'hideOnContentClick' : false,
    'frameWidth' : 450,
    'frameHeight' : 430
  });

  $("a#toggle").click(function () {
    $("div#expanded_add_users").toggle("slow");
  });

});
