//= require old/user_filter

$(document).ready(function() {

  if (isOnPage('private_messages', 'index')) {

  $("a#toggle").click(function () {
    $("div#expanded_add_users").toggle("slow");
  });

  }

});
