$(document).ready(function() {

  $("#site_email_password_show").livequery('click', function(event) {
    if ($(this).is(':checked')) {
      changeInputType("#current_site_email_password", 'text');
    } else {
      changeInputType("#current_site_email_password", 'password');
    }
  });

});
